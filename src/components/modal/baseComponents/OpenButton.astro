---
const { class: className, ...rest } = Astro.props;
---

<button class:list={["openModalBtn", className]} {...rest}>
  <slot />
</button>

<style>
  button {
    border-color: transparent;
    background: transparent;
    color: inherit;
  }
</style>
